import styled from 'styled-components';

const Wrapper = styled.div`
  margin-top: 20px;

  button {
      padding: 15px 20px;
      font-size: 1.5rem;
  }

  span {
      padding: 10px;
      margin: 0 20px;
      font-size: 1.5rem;
  }
`;

export default Wrapper;